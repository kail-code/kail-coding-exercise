{
  "name": "diagnostic-screener",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/frontend && yarn build",
    "test": "yarn workspaces run test",
    "dev": "yarn workspaces run dev",
    "lint": "yarn workspaces run lint"
  }
}
